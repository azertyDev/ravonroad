# 004 — Задачи

Формат: `T-XXX | задача | файлы | готово когда | размер | [P] | зависимости`.

- **Размер**: `S` ≤ ~80 строк диффа, `M` ≤ ~200, `L` ≤ ~300.
- **[P]** — не пересекается с другими по файлам и не ждёт их результата.
- **⚡** — критично для всплеска: потолок канала ≈ 600 заявок/час при пике 1000
  (SRS §1.6, §6.7).
- **⚠** — требует решения владельца репозитория до старта (см. `spec.md`).

| ID | Задача | Файлы | Готово когда | Размер | [P] | Зависимости |
| --- | --- | --- | --- | --- | --- | --- |
| T-064 | Миграции `moderator`, `telegram_outbox`, `processed_update`, `bot_prompt`, `moderation_batch` (SRS §2.8, §2.10–§2.12, §2.15) | `apps/api/prisma/schema.prisma`, `prisma/migrations/0006_*`…`0008_*` | миграции накатываются на копию дампа; частичные индексы `moderator_active_idx` и `outbox_due_idx` созданы; `history_undo_once_idx` из 002 на месте | M | | T-022, T-024 |
| T-065 | Сверить и добить колонки `report` под переходы: `telegram_*_message_id`, `duplicate_of_id`, `status_reason*`, `publication_url`, `done_at` (SRS §2.2) | `apps/api/prisma/schema.prisma`, `prisma/migrations/0009_*` | все CHECK-инварианты SRS §2.2 присутствуют в БД; `CHECK ((status='DONE') = (done_at IS NOT NULL))` отклоняет несогласованную строку | S | | T-064 |
| T-066 | Первичный засев `moderator` из `TELEGRAM_MODERATOR_IDS` и документированный сниппет правки состава (ADR-0005, SRS §2.8) | `apps/api/prisma/seed/moderators.ts`, `docs/ops/moderators.md` | после первого развёртывания в таблице есть засеянные модераторы с `added_by_moderator_id = null`; сниппет `UPDATE` меняет состав без передеплоя и без изменения кода | S | | T-064 |
| T-067 | Webhook-контроллер и проверка `X-Telegram-Bot-Api-Secret-Token` (SRS §6.2, §9.3) | `apps/api/src/telegram/webhook.controller.ts`, `telegram.module.ts`, `secret.ts` | неверный секрет → `401`, разбор тела не начат, в логе `webhook_auth_failed`; сравнение через `timingSafeEqual` с предварительной сверкой длин; `setWebhook` регистрируется с `allowed_updates: ["message","callback_query"]` | M | | T-064 |
| T-068 | Идемпотентность апдейтов по `update_id` и осмысленные коды ответа (SRS §6.11) | `apps/api/src/telegram/idempotency.ts` | повторная доставка того же `update_id` не создаёт второго эффекта (`processed_update` вставляется в той же транзакции); транзиентная ошибка → `500`, логическая → `200` | S | | T-067 |
| T-069 | Кодек `callback_data` с версией схемы (SRS §6.4) | `apps/api/src/telegram/callback-data.ts` | unit: сериализация и разбор всех операций, длина ≤ 64 байт, мусор и чужая версия отклоняются без исключения | S | [P] | T-067 |
| T-070 | Сверка нажавшего с allowlist **до любой записи** (US-024, SRS §6.2, §9.4) | `apps/api/src/telegram/moderator.guard.ts` | интеграционный: нажатие пользователем вне allowlist не создаёт ни одной строки в БД, даёт приватный alert и событие `unauthorized_button_press`; в публичной истории заявки следа нет | M | | T-067, T-066 |
| T-071 | Машина переходов по таблице PRD §5.1 | `apps/api/src/reports/transitions.ts` | unit покрывает все пары «статус × действие»: 13 разрешённых выполняются, остальные отклоняются; таблица переходов — единственный источник истины, дублей условий в обработчиках нет | M | | T-022 |
| T-072 | ⚡ Outbox-воркер: `SKIP LOCKED`, backoff, обработка `429`, бюджет 20 сообщений/мин (SRS §6.7) | `apps/api/src/telegram/outbox.worker.ts`, `bot-api.client.ts` | при недоступной заглушке Bot API запись не теряется и уходит после восстановления; `429` с `retry_after` сдвигает `next_attempt_at` и не увеличивает `attempts`; `400` закрывает запись с `last_error`; интеграционный тест на соблюдение бюджета | L | | T-064 |
| T-073 | ⚡ Рендер карточки: медиагруппа с подписью + сообщение с кнопками (SRS §6.3, §6.4) | `apps/api/src/telegram/card.renderer.ts` | карточка содержит `RR-`номер, район, категорию, ориентир, дату, ссылку на точку; строка «⚠️ проверить» при сигналах антиабуза; строка «рядом: RR-…» при кандидатах в дубли; ни телефона, ни ника заявителя; подпись укладывается в 1024 символа | M | | T-072, T-069, T-034, T-026 |
| T-074 | ⚡ Постановка `CARD_CREATE` после обработки последнего `BEFORE`-фото (SRS §1.3 п.7) | `apps/api/src/media/photo.worker.ts` | карточка уходит после того, как последнее фото заявки стало `READY`, и не раньше; при `FAILED` части фото карточка всё равно уходит с оставшимися | S | | T-073, T-036 |
| T-075 | Одношаговые переходы: «Принять», «В работу», «Вернуть в очередь» (US-019, US-023) | `apps/api/src/telegram/handlers/status.handler.ts` | один тап меняет статус без дополнительных полей; кнопки перестроены под разрешённые из нового статуса; публичная карточка и `/z/<token>` показывают новый статус и запись в истории | M | | T-071, T-070 |
| T-076 | Гонка двух модераторов через условный `UPDATE` (US-026, SRS §6.6) | `apps/api/src/reports/transitions.ts` | интеграционный: два одновременных перехода — первый применён, второй получает alert «статус уже изменён на X»; ноль строк из `UPDATE` трактуется как отказ, а не как ошибка | S | | T-075 |
| T-077 | Обновление карточки после перехода и схлопывание неотправленных `CARD_EDIT` (US-026, SRS §6.6) | `apps/api/src/telegram/card.updater.ts` | в карточке виден новый статус, кто поставил и когда; неотправленные `CARD_EDIT` одной заявки заменяются последним; у терминальных статусов кнопок переходов нет | S | | T-075 |
| T-078 | Двухшаговые переходы с причиной из списка: `REJECTED`, `OUT_OF_SCOPE` (US-020, US-022, SRS §6.5) | `apps/api/src/telegram/handlers/reason.handler.ts` | нажатие заменяет кнопки статусов кнопками причин в том же сообщении, новых сообщений в группе нет; переход без причины не выполняется; после `REJECTED` заявка исчезает с карты, после `OUT_OF_SCOPE` остаётся с маркером реестра | M | | T-075 |
| T-079 | Текстовый ввод через `ForceReply`: «другое» и номер оригинала (US-021, SRS §6.5, §2.12) | `apps/api/src/telegram/handlers/prompt.handler.ts` | отвечать может только начавший переход; prompt старше часа даёт «время истекло»; номер несуществующей заявки, собственный номер и оригинал в статусе `DUPLICATE` отклоняются с сообщением | M | | T-078 |
| T-080 | Окно отмены 15 минут и отложенное снятие кнопки (PRD §5.4, SRS §6.10) | `apps/api/src/telegram/handlers/undo.handler.ts`, `apps/api/src/reports/undo.service.ts` | отменяет только автор перехода (сверка по `actor_telegram_user_id`); запись должна быть последней в истории и не отменённой; отмена `DONE` уменьшает счётчик и обнуляет `done_at`, фото остаются но публично не видны; отмена — новая строка истории; с подменой времени на +16 минут кнопки нет | L | | T-077, T-024 |
| T-081 | ⚡ Приём фото «после» ответом на карточку, буфер альбома 2 с (US-028, US-030, SRS §6.8) | `apps/api/src/telegram/handlers/after-photo.handler.ts` | 1–3 фото переводят `ACCEPTED`/`IN_PROGRESS` в `DONE`; альбом из нескольких апдейтов собирается по `media_group_id`; 4-е фото отклонено с ответом; фото не ответом на карточку → подсказка; фото на заявку в другом статусе → текущий статус и причина отказа; заявка уже в `DONE` → фото добавляются, счётчик не растёт; allowlist не проверяется (PRD §12.4) | L | | T-072, T-036, T-071 |
| T-082 | Инвариант BR-006 в транзакции перехода в `DONE` (SRS §2.2, §11.3) | `apps/api/src/reports/transitions.ts`, `apps/api/test/invariants.spec.ts` | интеграционный: `DONE` без хотя бы одного фото «после» невозможен ни одним путём; кнопки `DONE` в клавиатуре не существует | S | | T-081 |
| T-083 | Ссылка на публикацию (US-029, SRS §6.9) | `apps/api/src/telegram/handlers/publication.handler.ts` | ссылка при `DONE` сохраняется и заменяет предыдущую целиком; допускаются только `http`/`https` длиной ≤ 512; в другом статусе бот отвечает, что ссылку можно приложить после закрытия; ссылка не рендерится как HTML нигде | S | | T-081 |
| T-084 | ⚡ Digest-режим при очереди больше 30 заявок (SRS §6.13, §2.15) | `apps/api/src/telegram/digest.service.ts` | при 31 неотправленной карточке партия уходит одним сообщением на 10 заявок со строкой на заявку и ссылкой на публичную карточку; фотографии в digest не вкладываются; кнопки «Принять все», «Отклонить все», «Раскрыть»; состав пакета зафиксирован в `moderation_batch` в момент отправки | L | | T-072, T-064 |
| T-085 | ⚡ Пакетные действия и их отмена (SRS §6.14) | `apps/api/src/telegram/batch.service.ts` | применяется только к заявкам в ожидаемом статусе, ответ называет «применено к N из M»; на каждую затронутую заявку своя строка истории; повторное нажатие отклонено по `applied_at`; отмена возвращает весь состав пакета одной кнопкой в пределах 15 минут | L | | T-084, T-080 |
| T-086 | ⚡ Кластеры дублей: одна карточка на кластер и приоритет очереди (SRS §6.14, §3.2) | `apps/api/src/telegram/duplicates.service.ts`, `queue-order.ts` | три точки в 30 м дают один кластер с одним корнем; «Все дубли RR-…» переводит в `DUPLICATE` всех, кроме корня, одним пакетом без второго шага; «Разные ямы» обнуляет `duplicate_candidate_of_id`, не трогая статусы; при формировании пакета крупные кластеры идут первыми, заявки с сигналами антиабуза последними, дальше FIFO | M | | T-085, T-026 |
