# 002 — Задачи

Формат: `T-XXX | задача | файлы | готово когда | размер | [P] | зависимости`.

- **Размер**: `S` ≤ ~80 строк диффа, `M` ≤ ~200, `L` ≤ ~300.
- **[P]** — не пересекается с другими по файлам и не ждёт их результата.
- **⚡** — критично для всплеска до 1000 заявок в час (SRS §1.6).
- **⚠** — требует решения владельца репозитория до старта (см. `spec.md`).

| ID | Задача | Файлы | Готово когда | Размер | [P] | Зависимости |
| --- | --- | --- | --- | --- | --- | --- |
| T-020 | Объявить контракт создания заявки и справочников (SRS §4.2, §4.6, ADR-0006) | `packages/shared-types/src/report.ts`, `src/catalog.ts`, `src/errors.ts` | экспортированы `CreateReportRequest`, `CreateReportResponse`, `DistrictDto`, `CategoryDto`, 7 новых `ErrorCode`; `CategoryDto.code` — `string`, не union (SRS §2.5); `pnpm typecheck` зелёный | S | | T-002 |
| T-021 | Миграция `district` + `category`, GiST-индекс, сид из GeoJSON и глоссария (SRS §2.5, §2.6) | `apps/api/prisma/schema.prisma`, `prisma/migrations/0002_*`, `apps/api/prisma/seed/*` | сид даёт 12 районов и категории глоссария; сумма `area_ha` совпадает с датасетом в пределах 0,1%; `district.geom` — `MultiPolygon` у всех 12 строк | M | | T-005 |
| T-022 | Миграция `report`: последовательность номера, индексы, CHECK-инварианты (SRS §2.1, §2.2) | `apps/api/prisma/schema.prisma`, `prisma/migrations/0003_*` | интеграционный тест: `INSERT` со статусом `DUPLICATE` без `duplicate_of_id` отклонён CHECK; `status='DONE'` без `done_at` отклонён; `nextval` выдаёт номер без `COUNT(*)` | M | | T-021 |
| T-023 | Миграция `report_photo`, `abuse_signal`, `form_open_counter` (SRS §2.4, §2.9, §2.13) | `apps/api/prisma/schema.prisma`, `prisma/migrations/0004_*` | 4-я строка фото одного вида отклонена `photo_slot_idx`; повтор `sha256` для той же заявки отклонён `photo_dedup_idx`; частичный `photo_queue_idx` создан | M | | T-022 |
| T-024 | Миграция `report_status_history` (SRS §2.7) | `apps/api/prisma/schema.prisma`, `prisma/migrations/0005_*` | первая запись истории с `from_status = null` создаётся; `history_undo_once_idx` отклоняет вторую отмену одного перехода | S | | T-022 |
| T-025 | Гео-модуль: point-in-polygon и геозабор одним `$queryRaw` (SRS §3.3, §2.14) | `apps/api/src/geo/geo.service.ts`, `geo.module.ts`, `packages/shared-types/src/geo.ts` | интеграционный: центр Чиланзара → `chilonzor`; эксклав `69.454308, 41.413567` → `mirzo-ulugbek`; Самарканд и Чирчик → `null`; ни одна контрольная точка не попала в два района | M | | T-021 |
| T-026 | Поиск вероятных дублей в 50 м и запись `duplicate_candidate_of_id` (SRS §3.2) | `apps/api/src/geo/duplicates.service.ts` | интеграционный: две точки в 30 м находят друг друга, в 300 м — нет; `REJECTED` в выдачу не попадает; новая заявка цепляется к самой ранней открытой (звезда, не граф) | M | | T-025, T-022 |
| T-027 | `GET /api/districts` и `GET /api/categories` (SRS §4.6) | `apps/api/src/catalog/*` | ответ содержит 12 районов с `bbox` и активные категории; заголовок `Cache-Control: public, max-age=86400` | S | | T-021, T-020 |
| T-028 | ⚠ S3-клиент: PUT, GET, DELETE, HEAD; схема ключей (SRS §5.5) | `apps/api/src/media/s3.service.ts`, `s3.module.ts`, `.env.example` | объект кладётся под `incoming/<uuid>` и читается обратно; финальные ключи имеют вид `photos/<sha[0:2]>/<sha>.jpg`; заголовки `Content-Type`, `Cache-Control: immutable`, `Content-Disposition: inline` выставлены | M | | T-004 |
| T-029 | ⚡ Приём multipart: лимиты на потоке, magic bytes, семафор 8 (SRS §4.2, §5.3, §5.7) | `apps/api/src/reports/upload.interceptor.ts`, `apps/api/src/reports/sniffer.ts` | unit: JPEG/PNG/WebP приняты, HEIC/SVG/PDF/ZIP/пустой файл → `415`; файл >4 МБ → `413`; 9-й одновременный запрос ждёт, затем `429` с `Retry-After` | M | | T-028 |
| T-030 | ⚡ Лимиты запросов в памяти процесса, скользящее окно (SRS §9.5) | `apps/api/src/common/rate-limit.*` | unit на границы окна и независимость адресов; 61-я заявка за час с адреса → `429`; 30 запросов/мин на `/api/track/*` зарезервированы конфигурацией | S | [P] | T-004 |
| T-031 | Генерация `tracking_token` (SRS §2.3) | `apps/api/src/reports/tracking-token.ts` | unit: 22 символа `base64url`, источник `node:crypto`, ноль повторов на 10⁶ выборках; `Math.random` в файле отсутствует | S | [P] | T-022 |
| T-032 | ⚡ `POST /api/reports`: геозабор до файлов, PUT сырых байт, одна транзакция, `201` (SRS §1.3, §4.2, §5.3) | `apps/api/src/reports/reports.controller.ts`, `reports.service.ts`, `reports.module.ts` | точка вне города → `400 OUTSIDE_TASHKENT`, 0 строк в БД и 0 объектов в S3; внутри города → `201` с `RR-`номером, токеном и `photosPending: true`; ответ не содержит `contact*`, `created_ip`, сигналов | L | | T-023, T-024, T-025, T-029, T-031 |
| T-033 | Идемпотентность по `Idempotency-Key` (SRS §4.2) | `apps/api/src/reports/reports.service.ts`, миграция уникального частичного индекса | повтор с тем же ключом → `200` с тем же телом и одна строка в `report`; тот же ключ с другим телом → `409 IDEMPOTENCY_CONFLICT` | S | | T-032 |
| T-034 | ⚡ Сигналы антиабуза: honeypot, скорость заполнения, мягкий лимит по IP (PRD §10.2–10.4, SRS §2.9) | `apps/api/src/reports/abuse.service.ts` | заполненный `website` создаёт строку `HONEYPOT` и заявку не отклоняет; отправка быстрее 5 с с тремя фото даёт `FAST_FILL`; 11-я заявка за час с адреса даёт `IP_RATE` и создаётся | M | | T-032, T-023 |
| T-035 | ⚡ Photo-воркер: очередь на `report_photo`, `FOR UPDATE SKIP LOCKED`, backoff, `FAILED` (SRS §5.4, ADR-0007) | `apps/api/src/media/photo.worker.ts` | заявка создаётся при остановленном воркере (`201`, `photosPending: true`); запущенный воркер доводит фото до `READY`; одна задача не выдаётся двум проходам; после 5 неудач `FAILED`, заявка остаётся видимой | L | | T-032, T-028 |
| T-036 | ⚠⚡ Перекодирование: `sharp`, вырезание EXIF, ориентация, превью 400 px, `sha256` по итоговым байтам (SRS §5.3 п.4–5, §5.5, §9.6) | `apps/api/src/media/process.service.ts` | `exiftool` по итоговому объекту не находит ни одного тега, включая GPS; фото 4000 px становится ≤1600 px; `limitInputPixels` отклоняет декомпрессионную бомбу; ключ совпадает с `sha256` содержимого | M | | T-035 |
| T-037 | `POST /api/form-opens` — суточный счётчик для метрики P-1 (SRS §2.13, §4.6) | `apps/api/src/stats/form-opens.*` | `204` с пустым телом, счётчик суток растёт `ON CONFLICT DO UPDATE`; лимит 60/час с адреса; ни cookie, ни идентификаторов не заводится | S | [P] | T-023, T-030 |
| T-038 | ⚠ Загрузчик Яндекс JS API v3 и виджет выбора точки (SRS §7.4, ADR-0004) | `apps/web/src/features/report-form/map-pin/*` | пин перетаскивается, координаты уходят в форму с 6 знаками; API грузится динамическим `import()` только на маршрутах с картой; ни одного обращения к геокодеру в сетевом логе | M | | T-018 |
| T-039 | Доступная альтернатива пину: «моё местоположение» и поля широты/долготы (US-007, PRD §8.2) | `apps/web/src/features/report-form/map-pin/*` | выбор точки выполняется целиком с клавиатуры; отказ в геопозиции форму не блокирует; числовые поля и пин синхронны в обе стороны | M | | T-038 |
| T-040 | ⚡ Клиентский даунскейл фото до 1600 px и конвертация в JPEG (SRS §5.2) | `apps/web/src/features/report-form/photo/*` | фото 4000 px становится ≤1600 px JPEG до отправки; HEIC вне iOS даёт понятную ошибку и не роняет форму; ошибка декодирования одного файла не отменяет остальные; unit на функцию масштабирования | M | | T-018 |
| T-041 | Форма заявки: поля, валидация, honeypot, счётчик символов, предпросмотр (US-006, 008–011, 017) | `apps/web/src/features/report-form/*`, `apps/web/src/routes/$locale/new.tsx` | 4-й файл отклонён с сообщением; без категории и без фото отправка не проходит; фокус переходит на первое незаполненное поле; точка вне города даёт сообщение о рамках кампании; рядом с контактами написано, что они не публикуются | L | | T-040, T-039, T-027, T-020 |
| T-042 | ⚡ Черновик в IndexedDB и `Idempotency-Key` (US-016, SRS §7.6) | `apps/web/src/features/report-form/draft.ts` | обрыв сети сохраняет поля, координаты и сжатые фото; возврат в течение 24 часов восстанавливает их с уведомлением и кнопкой «очистить»; через 24 часа черновик удалён; три нажатия «повторить» создают одну заявку | M | | T-041, T-033 |
| T-043 | Экран подтверждения: номер, ссылка `/z/<token>`, предупреждение, ожидание превью (US-012, SRS §7.5) | `apps/web/src/features/report-form/confirmation.tsx` | показаны `RR-`номер, ссылка и кнопка копирования; написано, что ссылку не восстановить; при `photosPending` заявка перезапрашивается каждые 3 с не более 10 раз, экран не блокируется спиннером | M | | T-042 |
| T-044 | ⚡ Интеграционные тесты приёма: гонка номеров, транзакционность, приватность ответа (SRS §11.3) | `apps/api/test/reports.spec.ts`, `test/privacy.spec.ts` | 100 параллельных `POST` дают 100 различных `public_number`; сбой на шаге фото не оставляет ни заявки, ни истории; появление `contact*`, `tracking_token`, `created_ip` в ответе ломает тест | M | | T-032, T-034, T-035 |
