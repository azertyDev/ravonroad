# 006 — Задачи

Формат: `T-XXX | задача | файлы | готово когда | размер | [P] | зависимости`.

- **Размер**: `S` ≤ ~80 строк диффа, `M` ≤ ~200, `L` ≤ ~300.
- **[P]** — не пересекается с другими по файлам и не ждёт их результата.
- **⚡** — критично для всплеска до 1000 заявок в час (SRS §1.6, §12.3).
- **⚠** — требует решения продакта до старта (см. `spec.md`).

| ID | Задача | Файлы | Готово когда | Размер | [P] | Зависимости |
| --- | --- | --- | --- | --- | --- | --- |
| T-098 | Correlation id через `AsyncLocalStorage` (SRS §8.3) | `apps/api/src/common/correlation.ts`, `request-id.middleware.ts` | идентификатор из тела ошибки находит запрос в логе одной командой; присланный клиентом `X-Request-Id` сохраняется; для апдейтов Telegram роль играет `update_id`, а `reportId` пишется рядом | M | | T-007 |
| T-099 | Структурные логи JSON в stdout и запретный список полей (SRS §8.4) | `apps/api/src/common/logger.ts`, `apps/api/test/log-shape.spec.ts` | один объект на строку с обязательными `ts`, `level`, `msg`, `correlationId`; тест падает при появлении `tracking_token`, телефона, ника, полного IP (только `/24`), содержимого фото или тела `multipart` | M | | T-098 |
| T-100 | Довести до лога 12 обязательных событий (SRS §10.2) | `apps/api/src/**` | каждое из 12 событий SRS §10.2 пишется в своей ситуации; проверка по списку зелёная; уровни расставлены по SRS §8.4 (`warn` — ожидаемый отказ, `error` — нужен человек) | M | | T-099 |
| T-101 | `/health/details` с кэшем внешних проверок на 60 с (SRS §10.1) | `apps/api/src/health/details.controller.ts` | ответ содержит состояние БД, S3 (`HEAD` бакета), Telegram (`getWebhookInfo`), глубину outbox и возраст последнего апдейта; секретов и персональных данных нет; при мёртвом S3 `/ready` по-прежнему `200` | M | | T-006, T-028, T-072 |
| T-102 | ⚡ Событие `queue_depth` в проходах обоих воркеров (SRS §10.3) | `apps/api/src/media/photo.worker.ts`, `apps/api/src/telegram/outbox.worker.ts` | при каждом проходе в лог уходят четыре числа: очередь обработки фото, очередь доставки, очередь модерации (`status='NEW'`) и число фото в `FAILED` | S | | T-035, T-072, T-099 |
| T-103 | ⚡⚠ Алерты в приватный чат координатора с дедупликацией (SRS §10.4) | `apps/api/src/telegram/alerts.service.ts`, `.env.example` | восемь условий SRS §10.4 дают сообщение в `TELEGRAM_ALERT_CHAT_ID`; условие, держащееся час, даёт одно сообщение, а не двенадцать (проверено подменой времени); алерты идут мимо `telegram_outbox`, чтобы не вставать в очередь, о которой сообщают | L | | T-102, T-072 |
| T-104 | Активная проверка `getWebhookInfo` каждые 5 минут (SRS §10.4) | `apps/api/src/telegram/webhook-health.service.ts` | сброшенный или чужой `url`, `pending_update_count > 20` и `last_error_date` моложе 10 минут дают алерт в пределах 5 минут | M | | T-103 |
| T-105 | `docs/metrics.sql` — метрики P-1…P-6 и North Star (SRS §10.3, PRD §1.3) | `docs/metrics.sql` | все метрики считаются запросами из файла; P-4 (`DONE` без фото «после») возвращает 0 строк; P-5 остаётся как страховка от миграции, ослабившей `NOT NULL` | S | [P] | T-022, T-024, T-037 |
| T-106 | Суточная задача очистки: IP 30 дней, контакты 90 дней, `processed_update` 7 дней, просроченные `bot_prompt` (SRS §9.8, §2.11, §2.12, PRD §9.2.5) | `apps/api/src/maintenance/cleanup.service.ts` | с подменой времени на +31 день `created_ip` пуст; на +91 день после терминального статуса контакты удалены, `contacts_deleted_at` проставлен, заявка и фото на месте; старые `processed_update` и `bot_prompt` удалены | M | | T-064, T-022 |
| T-107 | ⚠ Резервные копии `pg_dump` в S3, 14 копий (SRS §10.5) | `docker/backup/*`, `docker-compose.prod.yml` | суточный сжатый дамп появляется в S3 под отдельным префиксом; после 15 запусков копий ровно 14; последний дамп разворачивается на dev-машине, и приложение на нём работает; на dev задача не запускается (SRS §12.2 п.4) | M | | T-011, T-028 |
| T-108 | ⚡ Нагрузочная проверка порогов PRD §8.1 на сиде из 5000 заявок (SRS §11.5) | `scripts/seed-load.ts`, `docs/perf-baseline.md` | измерены p95 LCP главной, интерактивности карты, применения фильтра, открытия карточки и отправки формы с 3 фото; результаты сопоставлены с порогами PRD §8.1; отклонения зафиксированы явно, а не замолчаны | M | | T-053, T-107 |
